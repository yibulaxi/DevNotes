<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.kk.customview">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_cat"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/NoTitle">
        <activity android:name=".ui.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.BookTutorialActivity"/>

        <activity android:name=".chapter.DrawBasicActivity"/>
        <activity android:name=".chapter.AnimActivity"/>
        <activity android:name=".chapter.ViewAnimActivity"/>
    </application>

</manifest>